{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "provider-mock.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: provider-mock
{{- end }}
