# Dev / kind overlay — single-node Temporal start-dev.
image:
  tag: "1.4.1"
  pullPolicy: IfNotPresent

service:
  type: NodePort
  uiNodePort: 30233

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi
