replicaCount: 1

image:
  repository: keep-temporal-worker
  tag: "0.1.1"
  pullPolicy: IfNotPresent

temporal:
  address: temporal-frontend:7233
  namespace: default
  taskQueue: keep-ops
  workerRoot: /data

keep:
  apiUrl: ""
  apiKey: ""
  apiKeySecretName: ""
  apiKeySecretKey: KEEP_API_KEY

gpu:
  mockUrl: ""
  alertEmailTo: "ops@ai-dc.local"

serviceAccount:
  create: true
  name: ""

resources: {}

# Safe defaults — no credentials in values.
env: {}
