workflow:
  id: incident-to-temporal
  name: Start Temporal remediation on incident
  description: >
    Starts a Temporal workflow when a Keep incident carries a reserved
    alert code (labels.code / incident.code).
  triggers:
    - type: incident
      events:
        - created
      cel: code != ""
  actions:
    - name: start-temporal-remediation
      provider:
        type: temporal
        config: "{{ providers.my-temporal }}"
        with:
          operation: start_workflow
          workflow_type: RemediateIncident
          task_queue: keep-ops
          workflow_id: "incident-{{ incident.id }}"
          arg:
            incident_id: "{{ incident.id }}"
            code: "{{ incident.code }}"
            name: "{{ incident.name }}"
            severity: "{{ incident.severity }}"
